import { SmToOverlayDirective } from './sm-to-overlay.directive';

describe('SmToOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new SmToOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
