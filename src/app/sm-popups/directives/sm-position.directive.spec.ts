import { SmPositionDirective } from './sm-position.directive';

describe('SmPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SmPositionDirective();
    expect(directive).toBeTruthy();
  });
});
